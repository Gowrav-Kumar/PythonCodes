class Solution:
    def reverseStr(self, s: str, k: int) -> str:
        l=[]
        for i in range(0,len(s),k):
            l+=[s[i:i+k]]
        for i in range(0,len(l),2):
            l[i]=l[i][::-1]
        return ''.join(l)


input:  "abcdef"
         2
output: "bacdfeg"
